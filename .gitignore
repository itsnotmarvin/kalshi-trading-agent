# Local credentials. Commit only the redacted template.
.env
.env.*
!.env.example
*.pem
*.key
*.p8
*.rsa

# Python/editor artifacts
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.pyright/
.DS_Store
.vscode/

# Runtime state and generated reports. data/reference remains versioned.
data/runtime/**
!data/runtime/.gitkeep
data/early_marks/
*.jsonl

# Local scratch files retained only for compatibility with older workspaces.
yay.txt
pooper.txt

# One-off full-codebase review dumps
codebase_for_review.txt
complete_sanitized_codebase.txt
data/memory.json
data/category_stats.json
